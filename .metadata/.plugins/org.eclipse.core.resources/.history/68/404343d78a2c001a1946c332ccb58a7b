//find digits from given string
//for eg. "Hello we a4r T65ting Shastra"

package Package1;

public class DigitsFromString {

	public static void main(String[] args) {

		String s = new String("Hello we a4r T65ting Shastra");

		int sum = 0;
		for (int i = 0; i < s.length(); i++) {

			char c = s.charAt(i);

			/*if (s.charAt(i) == '1' || s.charAt(i) == '2' || s.charAt(i) == '3' || s.charAt(i) == '4'
					|| s.charAt(i) == '5' || s.charAt(i) == '6' ||

					s.charAt(i) == '7' || s.charAt(i) == '8' || s.charAt(i) == '9') {
				
				int b=(int)c;
				
				sum = sum + b; //doubt of printing sum
			}
*/
		if(s.charAt(i)>='0' && s.charAt(i)<='9') {
			//System.out.println(s.charAt(i));
			//System.out.println((int)'0');
			//int a = s.charAt(i)-'0';
			sum=(sum+s.charAt(i)-'0');
			//System.out.println(sum);
		}
		}

		System.out.println(sum);

	}

}

/*
 * char c=s.charAt(i); if(c>='0' && c<='9') { System.out.print(c); sum=sum+c; }
 */
